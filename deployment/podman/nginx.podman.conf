#=========================================================================#
# ⚠️ NE PAS UTILISER CE FICHIER — CONSERVE A TITRE DE REFERENCE UNIQUEMENT  #
#=========================================================================#
#
# Copier ce fichier dans /home/seo/conf/web/.../nginx.ssl.conf CASSE Nginx :
#   - l'IP y etait codee en dur (51.254.138.28) alors que le VPS ecoute sur
#     51.91.120.179  => bind() failed (99: Cannot assign requested address)
#   - HestiaCP REGENERE nginx.ssl.conf et ecrase toute modif manuelle
#     ("DO NOT MODIFY THIS FILE" en tete du fichier genere).
#
# >>> LA BONNE METHODE = template proxy HestiaCP <<<
#
#   En tant que debian (sudo) :
#     sudo bash /home/seo/404seo/deployment/hestia/install.sh \
#         --domain seo.404notfood.fr --user seo
#
#   Ce script installe le template container-proxy (.tpl/.stpl) qui injecte
#   AUTOMATIQUEMENT la bonne IP via %ip%, route /api/auth -> :3030,
#   /api -> :4000, le reste -> :3030, et n'est jamais ecrase par HestiaCP.
#
#   ⚠️ A faire SEULEMENT apres que les conteneurs tournent (sinon 502) :
#     bash /home/seo/404seo/deployment/podman/deploy-podman.sh --install-quadlet
#
# Le contenu de reference du reverse proxy se trouve desormais dans :
#   deployment/hestia/container-proxy.stpl   (HTTPS)
#   deployment/hestia/container-proxy.tpl    (HTTP)
#=========================================================================#
